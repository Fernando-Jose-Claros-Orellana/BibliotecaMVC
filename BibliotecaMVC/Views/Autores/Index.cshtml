@model IEnumerable<BibliotecaMVC.Models.Autor>
@{
    ViewData["Title"] = "Autores";
}

<h2>Catálogo de autores</h2>
<p>Total de autores: @Model.Count()</p>

<a asp-action="Create" class="btn btn-primary mb-3">Nuevo autor</a>

<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Nacionalidad</th>
            <th>Fecha de nacimiento</th>
            <th>Estado</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var autor in Model)
        {
            <tr>
                <td>@autor.Id</td>
                <td>@autor.Nombre</td>
                <td>@autor.Apellido</td>
                <td>@autor.Nacionalidad</td>
                <td>@autor.FechaNacimiento.ToString("dd/MM/yyyy")</td>
                <td>
                    @if (autor.Activo)
                    {
                        <span class="badge bg-success">Activo</span>
                    }
                    else
                    {
                        <span class="badge bg-danger">Inactivo</span>
                    }
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@autor.Id" class="btn btn-primary">Ver</a>
                    <a asp-action="Edit" asp-route-id="@autor.Id" class="btn btn-warning">Editar</a>
                    <a asp-action="Delete" asp-route-id="@autor.Id" class="btn btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
